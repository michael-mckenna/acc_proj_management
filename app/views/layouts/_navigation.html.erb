<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container">
	  <a class="navbar-brand" href="<%= user_path(current_user) %>">ACC Project Management</a>
	  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
	    <span class="navbar-toggler-icon"></span>
	  </button>

	  <div class="collapse navbar-collapse" id="navbarSupportedContent">
	    <ul class="navbar-nav mr-auto">
	      <li class="nav-item">
	        <a class="nav-link" href="<%= user_path(current_user) %>">Home</a>
	      </li>
	      <li class="nav-item">
	        <a class="nav-link" href="<%= projects_path %>">Projects</a>
	      </li>
	      <% if current_user.is_admin %>
	      <li class="nav-item">
	      	<a class="nav-link" href="<%= admin_path %>">Admin</a>
	      </li>
	      <% end %>
	 
	    </ul>
	    <ul class="navbar-nav">
		    <li class="nav-item">
		    	<% if current_user %>
			        <a class="nav-link" href="<%= signout_path %>">Sign Out</a>
		        <% else %>
					<a class="nav-link" href="<%= root_path %>">Sign Out</a>
		        <% end %>
		     </li>
	    </ul>
	  </div>
  </div>
</nav>