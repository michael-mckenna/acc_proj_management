<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container">
	  <a class="navbar-brand" href="<%= profile_path %>">ACC Project Management</a>
	  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
	    <span class="navbar-toggler-icon"></span>
	  </button>

	  <div class="collapse navbar-collapse" id="navbarSupportedContent">
	    <ul class="navbar-nav mr-auto">
	      <li class="nav-item">
	        <a class="nav-link" href="<%= profile_path %>">Home</a>
	      </li>
	      <li class="nav-item">
	        <a class="nav-link" href="<%= projects_path %>">Projects</a>
	      </li>
	 
	    </ul>
	    <ul class="navbar-nav">
		    <li class="nav-item">
		    	<% if current_user %>
			        <a class="nav-link" href="<%= signout_path %>">Sign Out</a>
		        <% else %>
					<a class="nav-link" href="<%= root_path %>">Sign Out</a>
		        <% end %>
		     </li>
	    </ul>
	  </div>
  </div>
</nav>